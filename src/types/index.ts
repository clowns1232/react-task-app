export interface ModalType {
  taskId: string;
  taskName: string;
  taskDescription: string;
  taskOwner: string;
}

export interface LogType {
  logId: string;
  logAuthor: string;
  logMessage: string;
  logTimestamp: string;
}
